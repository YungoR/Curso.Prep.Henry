name: Build Recordatorio IURIX APK

on:
  workflow_dispatch:
  push:
    branches: [ iurix-monitor ]
    paths:
      - 'IURIX_Monitor_SL/**'
      - '.github/workflows/iurix-build.yml'

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: IURIX_Monitor_SL
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'
      - name: Build debug APK
        run: gradle assembleDebug --no-daemon
      - name: Rename APK
        run: cp app/build/outputs/apk/debug/app-debug.apk Recordatorio-IURIX-v0.5.apk
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Recordatorio-IURIX-v0.5
          path: IURIX_Monitor_SL/Recordatorio-IURIX-v0.5.apk
          retention-days: 7
